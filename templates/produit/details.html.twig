{% extends 'base.html.twig' %}

{% block body %}

    <h2>page de detail</h2>

        <div><h3>{{ produit.libelle }}</h3> <strong> catégorie:</strong> {{ produit.categorie.libelle }}  <strong> référence:</strong> {{ produit.reference }}<strong> prix:</strong> {{ produit.prix }}
            <strong>  stock:</strong> {{ produit.stock }}, <strong> date d'ajout:</strong> {{ produit.getDateAjout()|date("d/m/y") }} </div>
    <form action="{{ path('produit_delete',{'id':produit.id}) }}">
        <button>delete </button>
    </form>
    <button>edit</button>


{% endblock %}

{% block title %}

{% endblock %}
